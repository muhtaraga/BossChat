/** @type {import('next').NextConfig} */
const nextConfig = {
  // Socket.io ve dosya sunumu custom server (server/index.ts) üzerinden yönetiliyor.
  reactStrictMode: true,
};

export default nextConfig;
